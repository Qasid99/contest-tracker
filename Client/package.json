{
  "name": "trackcontest",
  "version": "1.0.0",
  "description": "Coding contest tracker",
  "main": "app.js",
  "engines": {
    "node": "14.18.0",
    "npm": "8.14.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "app": "nodemon app",
    
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix trackcontest && npm run build --prefix trackcontest",
    "client-install": "cd trackcontest && npm intall",
    "client-build": "cd trackcontest && npm run build"
  },
  "author": "Qasid Shoaib",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.19"
  }
}
